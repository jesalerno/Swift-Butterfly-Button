# Copyright 2026 John Salerno.

name: CI

on:
  push:
    branches: ["main", "codex/**"]
  pull_request:

jobs:
  macos-test:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Swift version
        run: swift --version

      - name: Build
        run: swift build --build-tests

      - name: Lint
        run: ./scripts/lint.sh

      - name: Unit tests
        run: swift test

      - name: Vulnerability scan
        run: ./scripts/vulnerability_scan.sh

      - name: Upload vulnerability report
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-report
          path: reports/vulnerability_report.*

  ios-simulator-build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build iOS demo for iPhone simulator
        run: |
          xcodebuild -workspace ButterflyButton.xcworkspace \
            -scheme ButterflyButtoniOSDemo \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath .build/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            build

  ipados-simulator-build:
    runs-on: macos-15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build iOS demo for iPad simulator
        run: |
          xcodebuild -workspace ButterflyButton.xcworkspace \
            -scheme ButterflyButtoniOSDemo \
            -destination 'generic/platform=iOS Simulator,name=iPad' \
            -derivedDataPath .build/DerivedData \
            CODE_SIGNING_ALLOWED=NO \
            build
